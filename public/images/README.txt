Run ../../scripts_download_images.sh (from the project root, needs internet)
to populate this folder with the 16 real food/restaurant photos the app
uses. Until then, the app automatically falls back to generated colored
placeholder tiles, so it still renders correctly either way.
